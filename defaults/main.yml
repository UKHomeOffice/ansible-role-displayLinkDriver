---
#driver_download_url points to the specific version mentioned below, so driver_download_url needs to be updated if version number is
display_link_version: 1.3.52
driver_file_name: displaylink-driver-{{ display_link_version }}.run
